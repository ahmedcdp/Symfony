

{% extends "CDPBookingBundle::layout.html.twig" %}

{% block body %}

<h3>Formulaire de réservation</h3>

<div class="well">
  {{ form_start(form,{'action': path('cdp_booking_add'), 'method': 'POST'} , {'attr': {'class': 'form-horizontal'}})   }}

  {# Les erreurs générales du formulaire. #}
  {{ form_errors(form) }}

    <div>
      {# On affiche tous les messages flash dont le nom est « info » #}
      {% for message in app.session.flashbag.get('notice') %}
        <div class="flash-notice">
          {{ message }}
        </div>
      {% endfor %}
    </div>

  <div>
    {{ form_label(form.date, "Date", {'label_attr': {'class': 'champs_label'}}) }}
    {{ form_errors(form.date) }}
      {{ form_widget(form.date, {'attr': {'class': 'champs_input'}}) }}
  </div>

  <div>
    {{ form_label(form.number, "Nombre de billets", {'label_attr': {'class': 'champs_label'}}) }}
    {{ form_errors(form.number) }}
      {{ form_widget(form.number, {'attr': {'class': 'champs_input'}}) }}
  </div>

  <div>
    {{ form_label(form.email, "Adresse email", {'label_attr': {'class': 'champs_label'}}) }}
    {{ form_errors(form.email) }}
      {{ form_widget(form.email, {'attr': {'class': 'champs_input'}}) }}
  </div>

   <div>
    {{ form_label(form.halfday, "Demi-journée", {'label_attr': {'class': 'champs_label'}}) }}
    {{ form_errors(form.halfday) }}
      {{ form_widget(form.halfday, {'attr': {'class': 'champs_input'}}) }}
  </div>


  {{ form_widget(form.valider, {'attr': {'class': 'btn btn-primary'}}) }}
 

  {{ form_rest(form) }}
  {{ form_end(form) }}

</div>

{% endblock %}